Web-Archiver that uses phantomJS to load and template web pages before saving their soucre and accessing internally linked web pages.

Requires:
  phantom-js

Install
  git clone
  npm install phantomjs

Usage:
  phantomjs render.js [urls_to_crawl]

